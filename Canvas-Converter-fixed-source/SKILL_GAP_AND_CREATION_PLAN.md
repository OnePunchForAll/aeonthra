# Skill Gap And Creation Plan

## Existing Repo-Local Skills Inspected

| Skill | Current value in this run |
| --- | --- |
| `assignment-intent-cleanroom` | Relevant for assignment wording truth but not the primary driver of this pass |
| `canvas-dom-recon` | Relevant for Canvas DOM resilience and capture selectors |
| `category-and-module-normalization` | Relevant for Canvas structural cleanup |
| `deterministic-concept-firewall` | Relevant for rejecting weak concept candidates |
| `deterministic-semantic-forge` | Core semantic pipeline workflow |
| `distinct-evidence-scoring` | Relevant for evidence ranking and field support |
| `ember-memory` | Useful for deterministic adaptive memory, not primary in this pass |
| `fixture-golden-tests` | Core regression workflow for deterministic outputs |
| `golden-regression-fixtures` | Supports broader fixture maintenance |
| `offline-export-determinism` | Relevant for bundle export stability |
| `omega-self-repair-loop` | Relevant for repeated audit -> repair -> verify loops |
| `pdf-text-forensics` | Peripheral to this Canvas-first pass |
| `reforge-qa` | Core reproduce -> test -> patch workflow |
| `source-quality-gate` | Relevant for semantic truth gating |
| `ui-systems-polish` | Core for Atlas and shell UX refinement |

## Missing Skills To Create

| Skill | Invocation mode | Why it materially improves this run |
| --- | --- | --- |
| `bridge-contract-auditor` | Explicit-only | Keeps bridge fixes bound to emitted shape, accepted shape, and precise failure text |
| `extension-canonicality-checker` | Explicit-only | Prevents stale source and unpacked-extension folder drift |
| `payload-validator-trace` | Explicit-only | Forces queue, parse, and validation analysis instead of generic import debugging |
| `semantic-distinctness-forge` | Safe implicit | Centralizes the anti-repetition rules for concept-facing fields and downstream artifacts |
| `evidence-lane-provenance` | Explicit-only | Forces field-level provenance and support quality instead of vague evidence claims |
| `shell-mapper-truth-gate` | Safe implicit | Prevents shell practice and dominant concept selection from feeding on derived text |
| `atlas-skill-tree-architect` | Safe implicit | Keeps Atlas changes skill-first, derived, and deterministic |
| `assignment-readiness-honesty` | Safe implicit | Prevents readiness claims without prerequisite evidence |
| `purge-canonicalizer` | Explicit-only | Ensures deletion and preservation choices are evidence-based |
| `regression-orchestrator` | Safe implicit | Keeps fixture, contract, and UI regressions aligned through each loop |
| `release-truth-auditor` | Explicit-only | Verifies what is actually proven at stop time |
| `docs-truth-aligner` | Safe implicit | Aligns docs and ledgers to runtime truth after the fixes |
| `final-judge` | Explicit-only | Provides hard stop criteria after repeated loops |
| `command-capability-operator` | Explicit-only | Keeps capability claims tied to real thread surfaces |

## Usage Plan

1. Use `command-capability-operator` while finalizing the capability matrix and human trigger sequence.
2. Use `bridge-contract-auditor`, `extension-canonicality-checker`, and `payload-validator-trace` before changing the bridge.
3. Use `semantic-distinctness-forge`, `evidence-lane-provenance`, and `shell-mapper-truth-gate` during the semantic overhaul.
4. Use `atlas-skill-tree-architect` and `assignment-readiness-honesty` during Atlas derivation and UI changes.
5. Use `purge-canonicalizer`, `regression-orchestrator`, `release-truth-auditor`, `docs-truth-aligner`, and `final-judge` during the hardening loops.

## Created In This Pass

All planned missing skills were created under `.agents/skills` and used as boundaries for this run:

- `bridge-contract-auditor`
- `extension-canonicality-checker`
- `payload-validator-trace`
- `semantic-distinctness-forge`
- `evidence-lane-provenance`
- `shell-mapper-truth-gate`
- `atlas-skill-tree-architect`
- `assignment-readiness-honesty`
- `purge-canonicalizer`
- `regression-orchestrator`
- `release-truth-auditor`
- `docs-truth-aligner`
- `final-judge`
- `command-capability-operator`
